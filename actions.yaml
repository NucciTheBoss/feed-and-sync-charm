# Copyright 2022 Canonical
# See LICENSE file for licensing details.

ping:
  description: >
    Start a game of ping pong with the other units in the model.
  params:
    token:
      description: Token to pass between units.
      type: string
      default: ""
  required: [token]
  additionalProperties: false
  type: object
